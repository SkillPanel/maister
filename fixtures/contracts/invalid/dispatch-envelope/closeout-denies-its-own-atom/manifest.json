{
  "id": "closeout-denies-its-own-atom",
  "contracts": [
    "C2"
  ],
  "verdict": "invalid",
  "synthetic": true,
  "pseudonymized": false,
  "source": "synthetic",
  "writer_version": null,
  "sampled_at": "2026-09-13T09:14:22Z",
  "files": [
    {
      "path": "closeout-denies-its-own-atom.yml",
      "schema": "dispatch-envelope.schema.json#"
    }
  ],
  "expect": {
    "error_path": "/closeout_contract/pr_required",
    "error_keyword": "closeout-unreachable"
  },
  "notes": "The contradictory half of the pair. Every field is well typed, so the schema has nothing to say: the defect is between two fields. auto-high is the tier where a denial is final, which is what makes the contract unreachable rather than merely held for approval. The sound half is synthetic/dispatch-envelope/auto-high.envelope.yml, identical but for the atom."
}
