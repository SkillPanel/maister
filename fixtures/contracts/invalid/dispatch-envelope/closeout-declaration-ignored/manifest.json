{
  "id": "closeout-declaration-ignored",
  "contracts": [
    "C2"
  ],
  "verdict": "invalid",
  "synthetic": true,
  "pseudonymized": false,
  "source": "synthetic",
  "writer_version": null,
  "sampled_at": "2026-09-13T14:02:10Z",
  "files": [
    {
      "path": "closeout-declaration-ignored.yml",
      "schema": "dispatch-envelope.schema.json#"
    }
  ],
  "expect": {
    "error_path": "/closeout_contract/pr_required",
    "error_keyword": "closeout-declaration-ignored"
  },
  "notes": "The ignored half of the pair. Every field is well typed, so the schema has nothing to say: the defect is that the node stated a close-out contract in workflow.with and the envelope carries the opposite. attended is the tier where the derivation reaches pr_required: true, so it is the tier where an ignored declaration is invisible. The honoured half is synthetic/dispatch-envelope/declared-no-pr.envelope.yml, identical but for the contract it publishes."
}
