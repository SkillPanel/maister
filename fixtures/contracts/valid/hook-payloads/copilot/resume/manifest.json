{
  "id": "copilot-resume",
  "contracts": [
    "H1"
  ],
  "verdict": "valid",
  "synthetic": false,
  "pseudonymized": true,
  "source": "repo-self/development/2026-08-25-spikes",
  "writer_version": "1.0.80",
  "sampled_at": "2026-08-25T23:18:06Z",
  "files": [
    {
      "path": "payload.jsonl",
      "schema": null
    }
  ],
  "expect": {
    "platform_outcome": "fail-closed"
  },
  "notes": "Copilot CLI sessionStart + preToolUse payload captured live (Copilot CLI 1.0.80, headless -p with GITHUB_COPILOT_PROMPT_MODE_REPO_HOOKS=true). E2: hooks fire on --resume of a named session. sessionStart arrives with source \"resume\" and the swapped-in deny hook denied apply_patch, bash and skill. sessionId is identical across the resume, which is why the gate hook keys on cwd, never on the session id. The class fired 4 times; every invocation is kept, one JSON object per line. platform_outcome documents what the harness did, not what the maister hook decides."
}
