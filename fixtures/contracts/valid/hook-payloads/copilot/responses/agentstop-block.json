{
  "decision": "block",
  "reason": "gate node 'approve' is running but <run>/gates/approve.request.yml does not exist: suspend the run with one gate-request call (it writes the request file, the gate index and gate_pending together \u2014 there is no second state write), rewrite dashboard-data.js, print GATE-PENDING: approve, then stop."
}
