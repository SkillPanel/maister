# Node prose for the eject beside this file

A `direct:` node is executed by the engine from its own section in the prose
companion beside the definition that declares it. An eject is a definition, so
it carries its own companion or its `direct:` nodes resolve against nothing.

The sections here are stubs. What this fixture pins is the resolved shape of
the graph, not the prose that runs inside its nodes - that prose ships beside
the built-in and is asserted there.

## `research-foundation`

Executed from the shipped companion.

## `optional-phases-decision`

Executed from the shipped companion.

## `solution-convergence`

Executed from the shipped companion.

## `high-level-design`

Executed from the shipped companion.

## `completion`

Executed from the shipped companion.
