{
  "id": "copilot-stop-running-with-request",
  "contracts": [
    "H1",
    "E2"
  ],
  "verdict": "valid",
  "synthetic": true,
  "pseudonymized": false,
  "source": "synthetic",
  "writer_version": null,
  "sampled_at": "2026-08-25T23:18:06Z",
  "files": [
    {
      "path": "payload.json",
      "schema": "hook-payloads.schema.json#/$defs/copilot_agentstop"
    }
  ],
  "expect": {
    "replay": {
      "provider": "copilot",
      "event": "agentStop",
      "decision": "allow",
      "exit": 0,
      "stdout": "empty",
      "state_fixture": "synthetic/gate/pending",
      "cwd_layout": "umbrella-run"
    }
  },
  "notes": "The same running gate node, but the request file exists: the engine did ask. The nudge allows the stop with empty stdout. The replay harness mounts state_fixture at cwd_layout under a temporary directory and rewrites the payload cwd and every absolute path in the tool input to that mount point; the paths below are written against the canonical layout <cwd>/.maister/umbrella/runs/<run-id>/."
}
