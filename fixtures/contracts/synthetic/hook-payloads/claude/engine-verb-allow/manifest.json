{
  "id": "claude-engine-verb-allow",
  "contracts": [
    "H1"
  ],
  "verdict": "valid",
  "synthetic": true,
  "pseudonymized": false,
  "source": "synthetic",
  "writer_version": null,
  "sampled_at": "2026-09-14T19:40:00Z",
  "files": [
    {
      "path": "payload.json",
      "schema": "hook-payloads.schema.json#/$defs/claude_pretooluse"
    }
  ],
  "expect": {
    "replay": {
      "provider": "claude",
      "event": "PreToolUse",
      "decision": "allow",
      "exit": 0,
      "stdout": "json",
      "state_fixture": "synthetic/gate/answered",
      "cwd_layout": "umbrella-run"
    }
  },
  "notes": "No gate is pending, and the call is the plugin invoking its own state writer at its own verified path - the one allow that carries a body instead of being silence, so a session is not asked to approve the plugin's own writes. The invocation is written as the skill documents it, with the plugin-root variable unexpanded; the replay harness exports that variable the way a session does. Anything else - another path, another verb, a compound command - stays silent and reaches the operator's own permission rules."
}
