{
  "id": "performance-a",
  "contracts": [
    "A1",
    "A2",
    "A4",
    "A6"
  ],
  "verdict": "valid",
  "synthetic": true,
  "pseudonymized": false,
  "source": "synthetic",
  "writer_version": null,
  "sampled_at": "2026-08-25T23:18:06Z",
  "files": [
    {
      "path": "orchestrator-state.yml",
      "schema": "orchestrator-state.schema.json#"
    },
    {
      "path": "dashboard-data.js",
      "schema": "dashboard-data.schema.json#"
    }
  ],
  "strict_a2": true,
  "dashboard_html_md5": "c45365ee20c4875a63fd063d977aff41",
  "notes": "Synthesized from performance/SKILL.md:354-388 (performance_context field set) plus the shared verification_context block. No real >= 2.2.3 performance run existed at freeze time: replace this fixture with the first one that does (program-log item). Re-checked 2026-09-11 against every run on the authoring machine: five real performance runs exist, all predating the dashboard - none carries a dashboard-data.js at all, so none can stand in for a fixture that pins the state document and the dashboard together, and their age puts them below the compatibility floor. Three of the five do validate against the state schema unchanged, so the shape this fixture claims is not in doubt; what is still unproven is that a writer at or above the floor produces it. The fixture stays synthetic until a real run of this workflow is made at the current version. The suite says so on every run rather than leaving it in this note."
}
